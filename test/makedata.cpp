#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("test.in", "w", stdout);
    int n = 1e5, m = 1e5;
    cout << 1;
    while (n--)
        cout << 1;
    cout << '\n';
    cout << 1;
    while (m--)
        cout << 1;
    return 0;
}